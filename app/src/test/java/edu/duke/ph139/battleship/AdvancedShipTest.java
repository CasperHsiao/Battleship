package edu.duke.ph139.battleship;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

public class AdvancedShipTest {
  /*
  @Disabled
  @Test
  public void test_getIndexOfShipCoordinate(Coordinate c) {
    AbstractShipFactory<Character> f = new V2ShipFactory();
    AdvancedShip<Character> sub = f.makeSubmarine(new Placement("A0h"));
    assertEquals(1, sub.getIndexOfShipCoordinate(new Coordinate(0, 1)));
  }
  */

}
